{
  "name": "agentos-integrations",
  "version": "0.1.0",
  "description": "Apps and connectors for AgentOS",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:schema": "vitest run tests/schema.test.ts",
    "test:capabilities": "vitest run tests/capabilities.test.ts",
    "validate": "node tests/scripts/validate-schema.mjs --all",
    "lint:tests": "npx tsx tests/scripts/lint-tests.ts",
    "new-plugin": "npx tsx tests/scripts/new-plugin.ts",
    "test:changed": "./tests/scripts/test-changed.sh --staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "husky": "^9.1.7",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "yaml": "^2.8.2"
  },
  "dependencies": {
    "zod": "^4.3.5"
  }
}
