{
  "name": "agentos-integrations",
  "version": "0.1.0",
  "description": "Apps and connectors for AgentOS",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:schema": "vitest run tests/schema.test.ts",
    "test:changed": "./scripts/test-changed.sh --staged",
    "test:all": "./scripts/test-changed.sh --all",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "husky": "^9.1.7",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "yaml": "^2.8.2"
  }
}
