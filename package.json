{
  "name": "agentos-integrations",
  "version": "0.1.0",
  "description": "Apps and connectors for AgentOS",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:structure": "vitest run tests/structure.test.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "husky": "^9.1.7",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
